# pip.conf — Point pip at the Nexus pypi-group repo (trusted packages only)
#
# Install on developer machines:
#   Linux/Mac:  ~/.config/pip/pip.conf  (or /etc/pip.conf for system-wide)
#   Windows:    %APPDATA%\pip\pip.ini
#
# Replace <nexus-host> with your Nexus server hostname/IP.

[global]
index-url = https://<nexus-host>:8443/repository/pypi-group/simple/
