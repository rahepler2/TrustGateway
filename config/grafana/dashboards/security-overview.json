{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "fixed", "fixedColor": "blue" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 5, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Total Scans",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS total FROM jobs;", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "fixed", "fixedColor": "green" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 5, "w": 6, "x": 6, "y": 0 },
      "id": 3,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Passed Scans",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS passed FROM jobs WHERE status = 'done';", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "fixed", "fixedColor": "red" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "red", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 5, "w": 6, "x": 12, "y": 0 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Failed / Quarantined",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS failed FROM jobs WHERE status = 'failed';", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "fixed", "fixedColor": "purple" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 5, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Unique Packages",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(DISTINCT package) AS unique_pkgs FROM jobs;", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "custom": { "axisBorderShow": false, "drawStyle": "bars", "fillOpacity": 80, "stacking": { "group": "A", "mode": "normal" } }, "mappings": [] },
        "overrides": [
          { "matcher": { "id": "byName", "options": "critical" }, "properties": [{ "id": "color", "value": { "fixedColor": "dark-red", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "high" }, "properties": [{ "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "medium" }, "properties": [{ "id": "color", "value": { "fixedColor": "yellow", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "low" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }] }
        ]
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 5 },
      "id": 10,
      "options": { "legend": { "calcs": ["sum"], "displayMode": "table", "placement": "right" }, "tooltip": { "mode": "multi" } },
      "title": "CVE Severity Breakdown",
      "type": "barchart",
      "targets": [{
        "rawSql": "SELECT package || '==' || version AS pkg, COALESCE((result->'trivy'->>'critical')::int, 0) AS critical, COALESCE((result->'trivy'->>'high')::int, 0) AS high, COALESCE((result->'trivy'->>'medium')::int, 0) AS medium, COALESCE((result->'trivy'->>'low')::int, 0) AS low FROM jobs WHERE result->'trivy' IS NOT NULL ORDER BY created_at DESC LIMIT 20;",
        "refId": "A",
        "format": "table"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "mappings": [] },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 5 },
      "id": 11,
      "options": { "legend": { "displayMode": "table", "placement": "right", "calcs": ["sum"] }, "tooltip": { "mode": "single" }, "pieType": "donut", "reduceOptions": { "calcs": ["sum"], "fields": "", "values": false } },
      "title": "SBOM — License Distribution",
      "type": "piechart",
      "targets": [{
        "rawSql": "SELECT license, COUNT(*) AS count FROM (SELECT jsonb_array_elements_text(result->'sbom'->'licenses') AS license FROM jobs WHERE result->'sbom'->'licenses' IS NOT NULL AND jsonb_array_length(result->'sbom'->'licenses') > 0) sub GROUP BY license ORDER BY count DESC LIMIT 15;",
        "refId": "A",
        "format": "table"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "custom": { "align": "auto", "displayMode": "auto" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "red", "value": 1 }] } },
        "overrides": [
          { "matcher": { "id": "byName", "options": "verdict" }, "properties": [{ "id": "custom.displayMode", "value": "color-text" }, { "id": "mappings", "value": [{ "type": "value", "options": { "pass": { "color": "green", "text": "PASS" }, "warn": { "color": "orange", "text": "WARN" }, "fail": { "color": "red", "text": "FAIL" }, "error": { "color": "purple", "text": "ERROR" } } }] }] },
          { "matcher": { "id": "byName", "options": "critical" }, "properties": [{ "id": "custom.displayMode", "value": "color-background-solid" }, { "id": "color", "value": { "fixedColor": "dark-red", "mode": "fixed" } }] },
          { "matcher": { "id": "byName", "options": "high" }, "properties": [{ "id": "custom.displayMode", "value": "color-background-solid" }, { "id": "color", "value": { "fixedColor": "orange", "mode": "fixed" } }] }
        ]
      },
      "gridPos": { "h": 10, "w": 24, "x": 0, "y": 13 },
      "id": 5,
      "options": { "showHeader": true, "footer": { "show": false }, "sortBy": [{ "displayName": "scanned", "desc": true }] },
      "title": "Recent Scan Jobs",
      "type": "table",
      "targets": [{
        "rawSql": "SELECT id, package, version, ecosystem, status, result->>'verdict' AS verdict, COALESCE((result->'trivy'->>'critical')::int, 0) AS critical, COALESCE((result->'trivy'->>'high')::int, 0) AS high, COALESCE((result->'trivy'->>'medium')::int, 0) AS medium, COALESCE((result->'sbom'->>'component_count')::int, 0) AS components, created_at AS scanned, finished_at - created_at AS duration FROM jobs ORDER BY created_at DESC LIMIT 50;",
        "refId": "A",
        "format": "table"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "custom": { "axisBorderShow": false, "drawStyle": "bars", "fillOpacity": 80, "stacking": { "group": "A", "mode": "normal" } }, "mappings": [] },
        "overrides": [
          { "matcher": { "id": "byName", "options": "done" }, "properties": [{ "id": "color", "value": { "fixedColor": "green", "mode": "fixed" } }, { "id": "displayName", "value": "Passed" }] },
          { "matcher": { "id": "byName", "options": "failed" }, "properties": [{ "id": "color", "value": { "fixedColor": "red", "mode": "fixed" } }, { "id": "displayName", "value": "Failed" }] }
        ]
      },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 23 },
      "id": 6,
      "options": { "legend": { "calcs": [], "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single" } },
      "title": "Scans Over Time",
      "type": "timeseries",
      "targets": [{
        "rawSql": "SELECT date_trunc('hour', created_at) AS time, status, COUNT(*) FROM jobs WHERE created_at > NOW() - INTERVAL '7 days' GROUP BY time, status ORDER BY time;",
        "refId": "A",
        "format": "time_series"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "fixed", "fixedColor": "semi-dark-blue" }, "mappings": [] },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 31 },
      "id": 12,
      "options": { "colorMode": "value", "graphMode": "area", "orientation": "auto", "reduceOptions": { "calcs": ["sum"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Total SBOM Components Analyzed",
      "type": "stat",
      "targets": [{
        "rawSql": "SELECT COALESCE(SUM((result->'sbom'->>'component_count')::int), 0) AS total_components FROM jobs WHERE result->'sbom'->>'component_count' IS NOT NULL;",
        "refId": "A",
        "format": "table"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "custom": { "align": "auto", "displayMode": "auto" }, "mappings": [] },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 31 },
      "id": 13,
      "options": { "showHeader": true, "footer": { "show": false } },
      "title": "Top Vulnerable Packages",
      "type": "table",
      "targets": [{
        "rawSql": "SELECT package, version, ecosystem, COALESCE((result->'trivy'->>'critical')::int, 0) AS critical, COALESCE((result->'trivy'->>'high')::int, 0) AS high, COALESCE((result->'trivy'->>'medium')::int, 0) AS medium, COALESCE((result->'trivy'->>'low')::int, 0) AS low, COALESCE((result->'trivy'->>'critical')::int, 0) + COALESCE((result->'trivy'->>'high')::int, 0) AS total_crit_high FROM jobs WHERE result->'trivy' IS NOT NULL ORDER BY total_crit_high DESC LIMIT 10;",
        "refId": "A",
        "format": "table"
      }]
    }
  ],
  "schemaVersion": 39,
  "tags": ["security", "trust-gateway"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "Gateway PostgreSQL", "value": "Gateway PostgreSQL" },
        "hide": 2,
        "includeAll": false,
        "name": "DS_GATEWAY_POSTGRESQL",
        "options": [],
        "query": "postgres",
        "type": "datasource"
      }
    ]
  },
  "time": { "from": "now-7d", "to": "now" },
  "title": "Trust Gateway — Security Overview",
  "uid": "trust-gateway-overview"
}
