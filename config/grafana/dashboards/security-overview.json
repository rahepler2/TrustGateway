{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 0, "y": 0 },
      "id": 1,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Total Scans",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS total FROM jobs;", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }, { "color": "red", "value": 1 }] } },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 6, "y": 0 },
      "id": 2,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Failed Scans (Quarantined)",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS failed FROM jobs WHERE status = 'failed';", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 12, "y": 0 },
      "id": 3,
      "options": { "colorMode": "value", "graphMode": "area", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Passed Scans",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(*) AS passed FROM jobs WHERE status = 'done';", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 6, "w": 6, "x": 18, "y": 0 },
      "id": 4,
      "options": { "colorMode": "value", "graphMode": "none", "justifyMode": "auto", "orientation": "auto", "reduceOptions": { "calcs": ["lastNotNull"], "fields": "", "values": false }, "textMode": "auto" },
      "title": "Unique Packages",
      "type": "stat",
      "targets": [{ "rawSql": "SELECT COUNT(DISTINCT package) AS unique_pkgs FROM jobs;", "refId": "A", "format": "table" }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "custom": { "align": "auto", "displayMode": "auto" }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 12, "w": 24, "x": 0, "y": 6 },
      "id": 5,
      "options": { "showHeader": true, "footer": { "show": false } },
      "title": "Recent Scan Jobs",
      "type": "table",
      "targets": [{
        "rawSql": "SELECT id, package, version, ecosystem, status, result->>'verdict' AS verdict, created_at, finished_at FROM jobs ORDER BY created_at DESC LIMIT 50;",
        "refId": "A",
        "format": "table"
      }]
    },
    {
      "datasource": { "type": "postgres", "uid": "${DS_GATEWAY_POSTGRESQL}" },
      "fieldConfig": {
        "defaults": { "color": { "mode": "palette-classic" }, "custom": { "axisBorderShow": false, "axisCenteredZero": false, "axisLabel": "", "barAlignment": 0, "drawStyle": "bars", "fillOpacity": 80, "lineWidth": 1, "pointSize": 5, "scaleDistribution": { "type": "linear" }, "showPoints": "auto", "spanNulls": false, "stacking": { "group": "A", "mode": "normal" } }, "mappings": [], "thresholds": { "mode": "absolute", "steps": [{ "color": "green", "value": null }] } },
        "overrides": []
      },
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 18 },
      "id": 6,
      "options": { "legend": { "calcs": [], "displayMode": "list", "placement": "bottom" }, "tooltip": { "mode": "single", "sort": "none" } },
      "title": "Scans Over Time",
      "type": "timeseries",
      "targets": [{
        "rawSql": "SELECT date_trunc('hour', created_at) AS time, status, COUNT(*) FROM jobs WHERE created_at > NOW() - INTERVAL '7 days' GROUP BY time, status ORDER BY time;",
        "refId": "A",
        "format": "time_series"
      }]
    }
  ],
  "schemaVersion": 39,
  "tags": ["security", "trust-gateway"],
  "templating": {
    "list": [
      {
        "current": { "selected": false, "text": "Gateway PostgreSQL", "value": "Gateway PostgreSQL" },
        "hide": 2,
        "includeAll": false,
        "name": "DS_GATEWAY_POSTGRESQL",
        "options": [],
        "query": "postgres",
        "type": "datasource"
      }
    ]
  },
  "time": { "from": "now-7d", "to": "now" },
  "title": "Trust Gateway — Security Overview",
  "uid": "trust-gateway-overview"
}
